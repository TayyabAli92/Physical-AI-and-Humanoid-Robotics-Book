import React from 'react';

// This component is kept for reference but not actively used
// since the AI agent is now implemented in Root.tsx as an iframe
const ChatbotWidget = () => {
  return null;
};

export default ChatbotWidget;